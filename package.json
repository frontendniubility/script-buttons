{
  "name": "script-buttons-plus",
  "displayName": "Script Buttons for pnpm/yarn/npm",
  "publisher": "jimbo",
  "repository": {
    "type": "git",
    "url": "https://github.com/frontendniubility/script-buttons"
  },
  "description": "Inspired by https://github.com/jwaterfall/script-buttons.git. it is upgrade from that.",
  "version": "1.1.2",
  "engines": {
    "vscode": "^1.86.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "npm",
    "pnpm",
    "yarn",
    "node",
    "script",
    "scripts",
    "button",
    "buttons",
    "shortcuts",
    "actions",
    "package.json",
    "script-buttons.json"
  ],
  "icon": "images/icon.png",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "script-buttons.refreshScripts",
        "title": "Refetch the scripts from your package.json file"
      }
    ],
    "configuration": {
      "title": "My Node Package Manager",
      "properties": {
        "NodePackageManager.name": {
          "type": "string",
          "enum": [
            "pnpm",
            "npm",
            "yarn",
            ""
          ],
          "default": "pnpm",
          "description": "My Node Package Manager"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "~20.11.15",
    "@types/vscode": "^1.86.0",
    "@typescript-eslint/eslint-plugin": "^6.20.0",
    "@typescript-eslint/parser": "^6.20.0",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.9",
    "eslint": "^8.56.0",
    "glob": "^10.3.10",
    "mocha": "^10.2.0",
    "typescript": "^5.3.3"
  }
}
